# モダン・ジャンケンマン (Modern Jankenman) ハンドオフドキュメント

## 1. プロジェクト概要
本プロジェクトは、80年代のレトロアーケードゲーム『ジャンケンマン』をWebで再現したミニゲームです。シンプルなじゃんけんに「倍増（Double-Up）」というギャンブル要素と、音声に合わせたリズム操作を組み合わせた体験を提供します。

## 2. 技術スタック
- **Frontend**: HTML5, CSS3 (Vanilla), JavaScript (Vanilla JS)
- **Hosting**: GitHub Pages
- **Assets**: MP3形式の音声素材

## 3. 主要機能とロジック
### A. ゲームフロー
1. **待機中 (`IDLE`)**: STARTボタンで開始。
2. **選択中 (`SELECTING`)**: `jankenpon.mp3` (初回) または `aikodesyo.mp3` (あいこ後) を再生し、ユーザーの手の選択を待つ。
3. **結果判定 (`RESULT`)**: ユーザーが選択した瞬間に音声を停止し、CPUの手と照合して勝敗を判定。
4. **倍増選択 (`DOUBLE_UP`)**: 勝利時のみ出現。「現在の報酬を確定して終了」か「さらに倍にするために次戦へ進む」かを選択。

### B. 音声・演出同期
- **リズム表示**: `startRhythmText()` 関数により、音声のリズムに合わせて「じゃん... けん... ぽん！！！」というテキストと画面フラッシュを同期させています。
- **即時停止**: ユーザーが手を選択した瞬間、`Audio.pause()` と `currentTime = 0` を行い、心地よいレスポンスを実現しています。

### C. データ永続化
- `localStorage` を使用して `jankenman_medals` というキーで合計所持メダル数を保存しています。

## 4. 設定パラメータ (`script.js`)
コード冒頭の `GAME_CONFIG` で以下の調整が可能です：
- `JUDGMENT_WINDOW`: タイミング判定を導入する場合の許容範囲（ms）。
- `BASE_REWARD`: 初回勝利時の基本報酬枚数。
- `MAX_DOUBLE`: 倍増の上限枚数（例：32枚）。

## 5. ファイル構造
```text
/ (root)
├── index.html      # メイン構造
├── style.css       # ネオン風レトロデザイン
├── script.js       # ゲームロジック全体
└── assets/
    └── sounds/     # 全ての音声素材 (.mp3)
```

## 6. 今後の拡張アイディア
- **タイミング判定の厳格化**: 現在は「選択した瞬間に判定」ですが、音声の「ぽん！」の瞬間にボタンを押させるリズムゲーム要素の追加。
- **アニメーションの強化**: メダル獲得時のパーティクル演出や、CPUの手の表示アニメーションの追加。
- **ランキング機能**: Firebaseなどの簡易的なバックエンドを導入し、最高獲得枚数を競うリーダーボードの実装。
- **難易度設定**: CPUが時々意地悪な手（プレイヤーの傾向を分析して出す）を出すロジックの追加。
